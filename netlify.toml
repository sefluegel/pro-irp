[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/build"

# If you have Netlify Functions, point functions directory here:
# [build]
#   functions = "backend/handlers"
